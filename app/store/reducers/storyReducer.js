import Actions from '../actions';

const storyReducer= (
  state = {
    data: undefined,
    error: undefined,
  },
  { type, payload },
) => {
  switch (type) {
      
  }
};

export default storyReducer;
